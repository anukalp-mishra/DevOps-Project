[myservers]
192.168.56.10 ansible_user=ubuntu

#Use the following command to test connectivity to all servers in the inventory file
#ansible myservers -i inventory.ini -m ping

#If everything is correct, you’ll get:
#192.168.56.10 | SUCCESS => {
#    "changed": false,
#    "ping": "pong"
#}
#If you get a timeout error, check your server status and network settings.
#If you get a permission denied error, check your SSH key and user settings.
#You can also test connectivity to a specific server by replacing myservers with the server's IP address or hostname.